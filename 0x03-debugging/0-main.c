#include "main.h"

/**
 *main - tests the function that tests if a value is negative or positive
 *with vaalue 0
 *
 * Return: 0
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
